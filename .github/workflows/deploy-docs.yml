name: Deploy DocC

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    container: swift:6.2
    steps:
      - uses: actions/checkout@v6

      - name: Build documentation
        run: |
          swift package --allow-writing-to-directory ./docs \
            generate-documentation --target NostrClient \
            --disable-indexing \
            --transform-for-static-hosting \
            --hosting-base-path swift-nostr-client \
            --output-path ./docs

      - name: Add LLM documentation files
        run: |
          cp llms.txt ./docs/llms.txt
          cp llms-full.txt ./docs/llms-full.txt

      - uses: actions/upload-pages-artifact@v4
        with:
          path: docs

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}documentation/nostrclient
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
